 <nav class="navbar navbar-dark bg-dark px-4">
        <a class="navbar-brand text-white" href="{{ url_for('index') }}">Inventory</a>
        <div class="d-flex">
            {% if current_user.is_authenticated %}
                <span class="text-white me-3">{{ current_user.username }} ({{ current_user.role }})</span>
                <a class="btn btn-outline-light" href="{{ url_for('logout') }}">Logout</a>
                <a class="btn btn-outline-light" href="{{ url_for('change_password') }}">Change Password</a>
                {% if current_user.role=='admin' %}
                <a class="btn btn-outline-light" href="{{ url_for('manage_users') }}">Users</a>
                {% endif %}
            {% endif %}
        </div>
    </nav>
